import { CustomComponentOutletDirective } from './custom-component-outlet.directive';

describe('CustomComponentOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomComponentOutletDirective();
    expect(directive).toBeTruthy();
  });
});
